backend:
  name: git-gateway
  branch: main

media_folder: "images"

collections:
  - name: "notices"
    label: "Notice List"
    folder: "notices"
    create: true
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Details", name: "body", widget: "markdown"}
      
